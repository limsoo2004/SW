'''
    작성일 : 2023년 11월 1일
    작성자 : 컴퓨터공학부 23학번 임수현
    설명 : LAB 7-5 함수는 튜플을 돌려줄 수 있다.

    반지름을 입력받아 원의 넓이와 둘레를 계산하시오.
    넓이와 둘레를 계산하는 함수를 작성하시오.
    함수는 넓이와 둘레를 함께 돌려줍니다.(return)

    [함수]
        3. 반지름을 받아서 매개변수에 저장한다.
        4. 넓이와 둘레를 계산한다.
        5. 넓이와 둘레를 돌려준다.(함수를 호출한 곳으로)
    [메인]
        1. 반지름을 입력받는다.
        2. 함수 호출 -> 반지름을 가지고 호출한다.
        6. 돌려받은 넓이와 둘레를 튜플로 저장한다.
            (넓이, 둘레)
        7. 출력한다.
'''
# 원의 넓이와 둘레를 계산하는 함수
def calculate(r):
    # 원의 넓이 계산
    area = 3.14 * r * r  # 파이(π) 값을 3.14로 가정합니다.

    # 원의 둘레 계산
    circum = 2 * 3.14 * r  # 파이(π) 값을 3.14로 가정합니다.

    # 넓이와 둘레를 튜플로 반환
    return (area, circum)

# 메인 함수
if __name__ == "__main__":
    # 반지름을 입력받음
    r = float(input("반지름을 입력하세요: "))

    # 함수 호출하여 넓이와 둘레를 계산
    result = calculate(r)

    # 결과 출력
    print(f"원의 넓이: {result[0]:.2f}")
    print(f"원의 둘레: {result[1]:.2f}")

radius = float(input("원의 반지름을 입력하시오: "))
(area,circum) = calculate(radius)
print(f"반지름이 {radius}인 원의 넓이는 {area:.2f}이고, 원의 둘레는 {circum:.2}이다.")

circum = calculate(radius)
print(f"반지름이 {radius}인 원의 넓이는 {circum[0]:.2f}이고, 원의 둘레는 {circum[1]:.2f}이다")